# Pystern Blot (v0.1 skeleton)

Run (dev):
```bash
python -m pysternblot
```

This skeleton includes:
- Workspace import (sha256 copy)
- Project load/save stubs
- Final Result preview placeholder (QGraphicsScene)
